import { isItemKeyFirearmWeapon } from "../registry";
import { ItemKey } from "../types";
import { getItemTier } from "./get-item-tier";

// export function getItemScore(key: ItemKey): number {
//   const tier = getItemTier(key);
  
//   if (isItemKeyFirearmWeapon(key)) {

//   }
// }