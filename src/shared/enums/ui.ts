export enum SCENE {
  DISCORD_AUTH = "discord-auth",
  CREATE_CHARACTER = "create-character",
  IN_GAME = "in-game",
}

export enum ELEMENT {
  INVENTORY = "inventory",
  CHAT = "chat",
  JSON = "json",
}
