generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  engineType      = "binary"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
} 