name = "yolo"

modules = ['js-module-v2']

resources = ["resource-control", "main", "dlc-chameleoncolor", "dlc-licenseplates", "qa-tools"]

debug = true

announce = false
host = '0.0.0.0'
port = 7788

[js-module]
extra-cli-args = [
  # resource restart in js module needs more memory
  "--max-old-space-size=8192",
]
